import test from 'node:test';import assert from 'node:assert/strict';import {descriptionFrom,shingles,similarity} from './content-quality.mjs';
test('description keeps sentences and whole words',()=>{assert.equal(descriptionFrom('A complete sentence. '+ 'Additional detail '.repeat(30)),'A complete sentence.');assert.ok(descriptionFrom('word '.repeat(80)).endsWith('word…'));assert.equal(descriptionFrom('Short copy.'),'Short copy.');});
test('normalized location substitutions expose template similarity',()=>{const template=place=>`Arrange plumbing service for your ${place} home or business. Tell our local team which fixtures are affected and when the problem started so we can discuss the next step.`;const a=shingles(template('Town A').replace('Town A','LOCAL')),b=shingles(template('Town B').replace('Town B','LOCAL'));assert.equal(similarity(a,b),1);assert.ok(similarity(a,shingles('A water heater inspection checks the appliance condition and connections. Describe temperature changes and visible leaking when you request assistance.'))<.75);});
test('insufficient text cannot generate a spurious similarity result',()=>assert.equal(similarity(shingles('short'),shingles('short')),0));
