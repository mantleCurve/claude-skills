export function descriptionFrom(text,max=160){const clean=String(text||'').replace(/\s+/g,' ').trim();if(clean.length<=max)return clean;const sentences=clean.match(/[^.!?]+[.!?](?:\s|$)/g)||[];let result='';for(const sentence of sentences){if((result+sentence).trim().length>max)break;result+=sentence;}if(result.trim())return result.trim();const cut=clean.slice(0,max-1);return cut.slice(0,cut.lastIndexOf(' ')).replace(/[,;:]$/,'')+'…';}
export function shingles(text,n=3){const words=String(text||'').toLowerCase().match(/[\p{L}\p{N}]+/gu)||[];if(words.length<10)return new Set();return new Set(words.slice(0,-n+1).map((_,i)=>words.slice(i,i+n).join(' ')));}
export function similarity(a,b){if(!a.size||!b.size)return 0;let same=0;for(const item of a)if(b.has(item))same++;return same/(a.size+b.size-same);}
